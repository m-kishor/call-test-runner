name: Greet the World

on: [push]

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Hello World step
      uses: m-kishor/test-runner@v1
